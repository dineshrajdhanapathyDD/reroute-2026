# Deployed Re:Route AI backend — Lambda Function URL (no 30s cap; supports the
# live Amazon Nova agent + Titan embeddings). Used by `npm run build`.
# Local dev uses the Vite proxy to :8000 instead.
VITE_API_BASE=https://ertldwrhpgsnbmg66muiftp73e0wskgr.lambda-url.us-east-1.on.aws
